const canvas=document.getElementById('space');if(canvas&&window.THREE){const s=new THREE.Scene(),c=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,2000);c.position.z=260;const r=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});r.setPixelRatio(Math.min(devicePixelRatio,2));r.setSize(innerWidth,innerHeight);const n=Math.min(5000,Math.floor(innerWidth*innerHeight/300)),g=new THREE.BufferGeometry(),p=new Float32Array(n*3),co=new Float32Array(n*3);for(let i=0;i<n;i++){let j=i*3;p[j]=(Math.random()-.5)*900;p[j+1]=(Math.random()-.5)*600;p[j+2]=(Math.random()-.5)*900;let q=Math.random()>.62;co[j]=q?0:.75;co[j+1]=q?.85:.8;co[j+2]=q?1:.82}g.setAttribute('position',new THREE.BufferAttribute(p,3));g.setAttribute('color',new THREE.BufferAttribute(co,3));const stars=new THREE.Points(g,new THREE.PointsMaterial({size:1.8,vertexColors:true,transparent:true,opacity:.8}));s.add(stars);function a(){requestAnimationFrame(a);stars.rotation.y+=.00028;stars.rotation.x+=.00006;r.render(s,c)}a();addEventListener('resize',()=>{c.aspect=innerWidth/innerHeight;c.updateProjectionMatrix();r.setSize(innerWidth,innerHeight)})}document.querySelectorAll('.event-card').forEach(x=>x.onclick=()=>{document.querySelectorAll('.event-card').forEach(y=>y.classList.remove('active-card'));x.classList.add('active-card')});